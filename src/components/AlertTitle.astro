---
export interface Props {
  class?: string;
}

const { class: className, ...props } = Astro.props;
---

<div
  data-slot="alert-title"
  class:list={["text-text-primary mb-1 text-base font-semibold", className]}
  {...props}
>
  <slot />
</div>
